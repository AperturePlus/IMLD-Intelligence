<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xenosoft.imldintelligence.module.identity.internal.repository.mybatis.UserRoleRelMapper">

    <sql id="BaseColumns">
        id,
        tenant_id,
        user_id,
        role_id,
        granted_by,
        granted_at
    </sql>

    <resultMap id="UserRoleRelResultMap" type="xenosoft.imldintelligence.module.identity.internal.model.UserRoleRel">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
        <result property="grantedBy" column="granted_by"/>
        <result property="grantedAt" column="granted_at"/>
    </resultMap>

    <select id="findById" resultMap="UserRoleRelResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM user_role_rel
        WHERE tenant_id = #{tenantId}
          AND id = #{id}
    </select>

    <select id="findByUserIdAndRoleId" resultMap="UserRoleRelResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM user_role_rel
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND role_id = #{roleId}
    </select>

    <select id="listByUserId" resultMap="UserRoleRelResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM user_role_rel
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
        ORDER BY id DESC
    </select>

    <select id="listByRoleId" resultMap="UserRoleRelResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM user_role_rel
        WHERE tenant_id = #{tenantId}
          AND role_id = #{roleId}
        ORDER BY id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_role_rel (
            tenant_id,
            user_id,
            role_id,
            granted_by,
            granted_at
        ) VALUES (
            #{tenantId},
            #{userId},
            #{roleId},
            #{grantedBy},
            #{grantedAt}
        )
    </insert>

    <update id="update">
        UPDATE user_role_rel
        SET user_id = #{userId},
            role_id = #{roleId},
            granted_by = #{grantedBy},
            granted_at = #{grantedAt}
        WHERE tenant_id = #{tenantId}
          AND id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user_role_rel
        WHERE tenant_id = #{tenantId}
          AND id = #{id}
    </delete>

    <delete id="deleteByUserIdAndRoleId">
        DELETE FROM user_role_rel
        WHERE tenant_id = #{tenantId}
          AND user_id = #{userId}
          AND role_id = #{roleId}
    </delete>

</mapper>
