<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xenosoft.imldintelligence.module.identity.internal.repository.mybatis.RoleMapper">

    <sql id="BaseColumns">
        id,
        tenant_id,
        role_code,
        role_name,
        description,
        status,
        created_at
    </sql>

    <resultMap id="RoleResultMap" type="xenosoft.imldintelligence.module.identity.internal.model.Role">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleName" column="role_name"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findById" resultMap="RoleResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM role
        WHERE tenant_id = #{tenantId}
          AND id = #{id}
    </select>

    <select id="findByRoleCode" resultMap="RoleResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM role
        WHERE tenant_id = #{tenantId}
          AND role_code = #{roleCode}
    </select>

    <select id="listByTenantId" resultMap="RoleResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM role
        WHERE tenant_id = #{tenantId}
        ORDER BY id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role (
            tenant_id,
            role_code,
            role_name,
            description,
            status
        ) VALUES (
            #{tenantId},
            #{roleCode},
            #{roleName},
            #{description},
            #{status}
        )
    </insert>

    <update id="update">
        UPDATE role
        SET role_code = #{roleCode},
            role_name = #{roleName},
            description = #{description},
            status = #{status}
        WHERE tenant_id = #{tenantId}
          AND id = #{id}
    </update>

    <update id="deleteById">
        UPDATE role
        SET status = 'INACTIVE'
        WHERE tenant_id = #{tenantId}
          AND id = #{id}
    </update>

</mapper>
